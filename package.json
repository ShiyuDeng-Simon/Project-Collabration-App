{
  "name": "project_collabration_app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "biuld": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server.server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4",
    "jwt-decode": "^4.0.0",
    "react-router-dom": "^6.21.1"
  }
}
